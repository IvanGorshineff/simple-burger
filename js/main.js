const burgerBtn = document.getElementById('burger')
const head = document.querySelector('.header')

document.addEventListener('DOMContentLoaded', function () {
  burgerBtn.onclick = () => {
    if (head.classList.toggle('open')) {
     
    }
   
  }
})